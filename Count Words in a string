reference: https://www.hackerrank.com/contests/smart-interviews/challenges/si-words-vowels-and-consonants/submissions/code/1316526095

initially i will have a flag  ,set to 1, and i will start iterating over the string till i find a non space character 
and once a found a non-space ,set flag to 0 and increment word count.

int f=1;
        if(s!=""){
        for(int i=0;i<s.size();i++)
        {
            if((s[i]-' ')==0){f=1;}
            else if(f==1){w++;f=0;}
        }
